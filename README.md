# flask-profile-uni
ITMO WAD HW 1 - Creating a profile using Flask, HTML, CSS

Bootstrap is used for responsive layout

The whole project is rendered via flask render_template, static html is in the root folder.

Bootstrap is served from external source, the same for fontawesome icons.
